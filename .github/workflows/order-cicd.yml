name: CI/CD — Order Service

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  AWS_REGION:  us-east-1
  ECR_REPO:    shopms-prod-order-service

jobs:
  build-and-deploy:
    name: Build, Push & Deploy Order Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID_PROD }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_PROD }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & Push Docker Image
        id: build
        env:
          REGISTRY:  ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          IMAGE_URI=$REGISTRY/$ECR_REPO:$IMAGE_TAG
          docker build -t $IMAGE_URI .
          docker push $IMAGE_URI
          docker tag $IMAGE_URI $REGISTRY/$ECR_REPO:latest
          docker push $REGISTRY/$ECR_REPO:latest
          echo "image=$IMAGE_URI" >> $GITHUB_OUTPUT

      # - name: Update K8s Deployment Image
      #   env:
      #     REGISTRY:  ${{ steps.login-ecr.outputs.registry }}
      #     IMAGE_TAG: ${{ github.sha }}
      #   run: |
      #     IMAGE_URI=$REGISTRY/$ECR_REPO:$IMAGE_TAG
      #     kubectl set image deployment/order-deployment \
      #       order-service=$IMAGE_URI \
      #       --namespace=sales